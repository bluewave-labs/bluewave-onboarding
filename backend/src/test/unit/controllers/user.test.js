const { describe, it } = require("mocha");

describe("Unit test user controller", () => {
  it.skip("getUsersList - if everything goes right, should return a list of users with pagination and status code 200", () => {});
  it.skip("getUsersList - if something goes wrong, should return status code 500 and the title error code GET_USER_LIST_ERROR", () => {});
  it.skip("getCurrentUser - if everything goes right, should return the user without the password", () => {});
  it.skip("getCurrentUser - if something goes wrong, should return status code 500 and the title error code GET_USER_ERROR", () => {});
  it.skip("updateUserDetails - if everything goes right, should return the updated user without the password", () => {});
  it.skip("updateUserDetails - if something goes wrong, should return status code 500 and the title error code UPDATE_USER_ERROR", () => {});
  it.skip("deleteUser - if everything goes right, should return the message 'User deleted successfully'", () => {});
  it.skip("deleteUser - if something goes wrong, should return status code 500 and the title error code DELETE_USER_ERROR", () => {});
});
